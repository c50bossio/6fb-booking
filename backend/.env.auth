# JWT Authentication Configuration for 6FB Booking
# Generate a secure JWT secret key with:
# python3 -c 'import secrets; print(secrets.token_urlsafe(64))'
JWT_SECRET_KEY=YOUR_64_CHAR_JWT_SECRET_KEY_HERE
AUTH_DATABASE_PATH=/path/to/your/auth-system/auth.db
